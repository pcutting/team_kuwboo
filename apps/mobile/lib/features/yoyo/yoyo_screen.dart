import 'package:flutter/material.dart';

/// Placeholder for the YoYo discovery screen.
class YoyoScreen extends StatelessWidget {
  const YoyoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('YoYo'),
      ),
    );
  }
}
