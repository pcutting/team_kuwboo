# App Store Connect / Apple Developer account identifiers.
#
# This file is safe to commit. The actual signing key (.p8 file) is kept
# outside the repo and passed to fastlane via environment variables in CI.

app_identifier("com.kuwboo.mobile")
team_id("5GQA38WHMY")

# Note: Apple ID is not set here because we authenticate with an App Store
# Connect API key (APP_STORE_CONNECT_API_KEY_ID / ISSUER_ID / P8 content)
# which is loaded from environment variables in the Fastfile.
