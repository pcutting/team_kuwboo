{
  "buildCommand": "cd apps/web && flutter build web --release --no-tree-shake-icons",
  "outputDirectory": "apps/web/build/web",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
